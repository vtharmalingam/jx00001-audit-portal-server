
# Run inside the container (/app) 


# ETL

# Question Index:
python -m app.etl.anchor_indexer.faiss_index_builder

# Test Functionalities

python -m app.procs.semantic_search.q_search_engine

python -m app.procs.question_evaluator
